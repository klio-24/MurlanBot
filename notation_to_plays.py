def notation_to_plays(data,deck):
    # 'data' is a string

    output = []

    for idx,i in enumerate(data):
        if data[idx] == "S" and data[idx+1] == "I"



        output.append(deck.index(decoded))

    return output